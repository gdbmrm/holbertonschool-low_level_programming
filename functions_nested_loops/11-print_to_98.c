#include "main.h"
#include <stdio.h>

void print_to_98(int n)
{
	int i;

	if (n == 98)
	{
		printf("%d", n);
	}
	if (n < 98)
	{
		for (i = 0; i <= (98 - n); i++)
		{
			printf("%d", n + i);
			if ( 

		}
	}
	printf("\n");
}
